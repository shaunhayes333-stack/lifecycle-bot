# AATE V5.7 Correctness Mandate — Source-Level Authority Convergence

**Status:** ACTIVE. Current head: **V5.0.6495** (commit `abe50b9d5`,
CI green — Build AATE APK + Runtime Smoke Test both pass).

**Mode:** PAPER ONLY. Live execution is intentionally disabled until
every mandate point remains green across consecutive operator dumps.

## Core doctrine

Every defect is fixed at its **source** — the originating state /
authority boundary — not by adding a passive read-only mirror or a
downstream guard. The 6469 → 6495 shipping stack is a monotone
implementation of that doctrine.

## The 26-point convergence

| # | Domain | Canonical authority | Ship | Status |
|---|--------|---------------------|------|--------|
| 1 | Position lifecycle single source | `CanonicalPositionAuthority6441` | 6441 | ✅ |
| 2 | Order size resolver | `OrderSizeResolver6441` + invariant `6468` | 6441 / 6468 | ✅ |
| 3 | Same-mint dedup | `SameMintDedupAuthority6441` | 6441 | ✅ |
| 4 | Forensic 17-field row schema | `ForensicExecutionRow6441` | 6441 | ✅ |
| 5 | Reward purity gate | `RewardPurityGate6441` (bus subscriber via `6453`) | 6441 / 6453 | ✅ |
| 6 | Learner runtime budget | `LearnerRuntimeBudgetGuard6441` | 6441 | ✅ |
| 7 | Reconciler QUICK / FULL | `CanonicalReconciler6441` + `WallClockReconciler6454` | 6441 / 6454 | ✅ |
| 8 | Root-cause attribution | `RootCauseTelemetry6441` + `RootCauseClassifier6471` (display in 6495) | 6441 / 6471 / 6495 | ✅ |
| 9 | Startup invariant gate | `StartupInvariantGate6441` | 6441 | ✅ |
| 10 | Acceptance invariant runner | `AcceptanceInvariantAudit6441` | 6441 | ✅ |
| 11 | Capital / equity read surface | `CanonicalCapitalAuthority6450` + `PaperEquityCalculator6467` | 6450 / 6467 | ✅ |
| 12 | Terminal close idempotency | `TerminalCloseIdempotencyLatch6450` + `TerminalSellIdempotency6464` | 6450 / 6464 | ✅ |
| 13 | Protective exit hot path | `ProtectiveExitScheduler6450` + `CanonicalRiskClock6454` | 6450 / 6454 | ✅ |
| 14 | Terminal sell CAS | `PositionStateLedger6454` (`reserveTerminalSell` / `confirmTerminalSell` / `abandonTerminalSell`) | 6454 | ✅ |
| 15 | Mint occupancy registry | `CanonicalMintOccupancyRegistry6464` | 6464 | ✅ |
| 16 | Lot quantity invariant | `CanonicalLotQuantity6464` (hardened 6470) | 6464 / 6470 | ✅ |
| 17 | Typed economic events | `EconomicEventSchema6464` + `EconomicOutcome6472` | 6464 / 6472 | ✅ |
| 18 | Canonical finalized bus | `CanonicalFinalizedTradeBus6464` + `FinalizedBusConsumerBridge6465` | 6464 / 6465 | ✅ |
| 19 | Canonical paper terminal pipeline | `CanonicalPaperTerminalBridge6469` + full paper-sell reducer (6474) | 6469 / 6474 | ✅ |
| 20 | Capital conservation tracer | `CapitalConservationTracer6469` + `CanonicalEconomicIdentity6470` | 6469 / 6470 | ✅ |
| 21 | Background trading authority | `BackgroundTradingAuthority6469` (wire remaining) | 6469 | 🟡 partial wire |
| 22 | Lifecycle projection watchdog | `CanonicalLifecycleAuthority6470` | 6470 | ✅ |
| 23 | Market data provenance | `MarketDataProvenance6471` | 6471 | ✅ |
| 24 | Adaptive lane damping | `LaneAdaptiveDamping6472` + `LaneAdmissionGate6473` | 6472 / 6473 | ✅ |
| 25 | Canonical instance identity stamp | `CanonicalInstanceIdentity6472` | 6472 / 6473 | ✅ |
| 26 | Executable route uniqueness + immutable lane election | 6487 + `V5.0.6494` sealed election | 6487 / 6494 | ✅ |

## V5.0.6495 source-fix batch (current head)

Four fixes at the layer where the bug was born:

1. **Paper → LIVE mistag** — `ExecutorCanonicalMirror6442` was hard-
   coding `paperMode=false` on the canonical open call. Fixed by
   forwarding the caller's `paperMode` to
   `CanonicalPositionAuthority6441.openPosition` /
   `promotePendingToOpen`.
2. **Root-cause display precedence** — `PipelineHealthCollector` now
   consults `RootCauseClassifier6471.classify()`; only
   `ECONOMIC_INTEGRITY` and `EXECUTION_FINALITY` tiers prepend to
   the display. Provider degradation can never again mask a capital
   breach.
3. **laneCap sentinel replaced** — `OrderSizeResolver6441.resolve()`
   default of `Double.MAX_VALUE` replaced with
   `DEFAULT_LANE_RISK_CAP_SOL = 5.0`. Diagnostic no longer renders
   `1.79e+308` / "$926M".
4. **USD/SOL mark scaling** — `BotService` mark-provider callback
   divides USD by `solPrice` before publishing to
   `CanonicalCapitalAuthority6450.installMarkProvider`. Equity
   snapshots stay SOL-denominated regardless of upstream USD price.

## Outstanding items (deferred, non-blocking)

- `rem` non-linear values on same `id` in partial-sell rows —
  `Executor.kt` / `PaperAccountLedger` audit pending. Likely
  resolved by 6495 USD scaling fix but not yet proven on a fresh
  operator dump.
- MOONSHOT exit-quality tuning — gated on ≥20 fresh clean closes
  with 100 % green canonical accounting.
- `BotService` START / STOP / rescue call sites routed through
  `BackgroundTradingAuthority6469`.
- Token-map single-flight consolidation.

## Compile / ship contract

- **NO LOCAL COMPILER.** Do not attempt `./gradlew` locally.
- Every change lands via `git push` → GitHub Actions CI.
- Verification pair: **Build AATE APK** green + **Runtime Smoke
  Test** green on the head SHA.
- Strict `BigInteger` / lamport math only. Doubles/floats forbidden
  on any accounting path.

## Growth doctrine

- Growth-Centric Runner Compounding: `RunnerCompoundingLadder6440`
  (11-tier ladder doubling every rung) blended with the confidence-
  driven sizer inside `EdgeOptimizer.calculatePositionSize`.
- Capital preservation creed: `CapitalPreservationCreed6439` +
  `LosingStreakReflex6439` + `AntiRewardHackingGuard6439` ensure bad
  behaviour is never reinforced.
- Paper ↔ live parity: `PaperLiveParityCreed6439` mandates that
  learning artefacts transfer with zero retraining at first live
  cutover.
