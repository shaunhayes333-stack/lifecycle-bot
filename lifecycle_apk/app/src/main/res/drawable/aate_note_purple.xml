<?xml version="1.0" encoding="utf-8"?>
<!--
  V5.0.7028 — the render's footer note.

  Most artboards end with a single tinted card explaining what the screen will
  do on its own, so the user is told the rule rather than left to infer it
  from the list above. rgba(139,92,246,.10) on a .30 edge, 15px radius.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="#1A8B5CF6"/>
    <stroke android:width="1dp" android:color="#4D8B5CF6"/>
    <corners android:radius="15dp"/>
</shape>
